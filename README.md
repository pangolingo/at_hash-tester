# OpenID Connect at_hash tester

Test that the `at_hash` claim in an ID token JWT matches the hash of an access token.

This is pretty rough right now, but could be eventually turned into some sort of app.